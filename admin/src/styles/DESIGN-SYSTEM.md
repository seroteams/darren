# Sero design system

**This file is no longer the reference.** It drifted (it still quoted 13px captions, 12px labels
and a 44-56px display, none of which are true) and two design docs is how the next drift starts.

Go to the one source:

- **The law:** [`DESIGN.md`](../../../DESIGN.md) at the repo root. Auto-loads every session.
- **The tokens:** [`design/tokens.css`](./design/tokens.css). `design.css` is only the `@import` barrel.
- **The visual twin:** the in-app component sheet, Admin → Design system
  ([`admin/src/stages/design.js`](../stages/design.js)).
- **The guard:** `npm run lint:tokens` (free).
