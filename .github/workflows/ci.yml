name: CI

# Harness H1 — auto-run the free ring on every change so nothing broken merges
# unnoticed. Runs only the $0, offline checks (no OPENAI_API_KEY, no paid gate).

on:
  pull_request:
  push:
    branches: [main]

# One in-flight run per ref; a newer push cancels the older run.
concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

jobs:
  free-ring:
    name: Free ring (typecheck · test · replay)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version-file: .node-version
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Typecheck (backend)
        run: npm run typecheck

      # The root tsconfig is Node/back-end only, and `vite build` transpiles without
      # typechecking — so without these two steps a type error in the code customers
      # actually run cannot fail any check, or the production build.
      - name: Typecheck (admin console)
        run: npm run typecheck:admin

      - name: Typecheck (customer app)
        run: npm run typecheck:customer

      - name: Tests (offline)
        run: npm test

      - name: Regression replay (offline)
        run: npm run replay
