{
  "_comment": "Per-stage OpenAI model selection. Stage env vars (OPENAI_MODEL_FOCUS_POINTS, OPENAI_MODEL_BANK, OPENAI_MODEL_PLANNER, OPENAI_MODEL_EVALUATION) override these at runtime for ad-hoc testing. The fallback order is: stage env → this config → OPENAI_MODEL env → hard-coded default.",
  "focus_points": "gpt-4o",
  "preparation": "gpt-4o",
  "bank": "gpt-4o",
  "planner": "gpt-4o",
  "evaluation": "gpt-4o"
}
