# frontend/

Placeholder for the **customer-facing app** (register / login + manager prep flow),
built in Phase 007. During the prototype phase the existing web UI lives in `../admin/`
(the internal admin tool), served by the API in `../backend/api/`.
