# Phase 3 — Design-loop polish (optional, on demand)

**Part of:** [plan.md](plan.md) · **Status:** ⬜ (only runs if Carl asks for any of it)

## Goal
Quality-of-life extras for the design loop — built piecemeal, only the bits Carl actually wants.

## Changes (menu, not a bundle)
- "Open real route ↗" link on each screen — jump from the gallery to the screen's normal address.
- Viewport width toggle (phone / tablet / desktop) around the host pane.
- Fetch-stub layer with per-screen variants — lets flow/list screens show designed **empty** and **error** states, not just happy-path data.

## Not in this phase
- Anything new beyond the menu above — park fresh ideas in plan.md.

## Done when
- [ ] Whichever menu items Carl picked work, screenshot-verified.
- [ ] Product owner tested and said go.

## Test scenarios — for the product owner
Written when Carl picks items from the menu — each item gets its own 1–2 scenarios then.
