# Phase 2 — Prep brief: verify + adopt readable display

## Scope
`PREPARATION` already has `assemblePreparation`. This phase confirms it renders in the new split display and that the **selected focus areas** show up in the user block.

## QA scenarios
1. Reach **Prep brief** → Sent tab shows system/user split + raw toggle.
2. The **user** section lists the focus area(s) picked on the previous step.
3. Go back, tick a *different* focus area, return to Prep brief → the user section reflects the new selection.

## Sign-off
- [ ] Product owner green light → commit (local).
