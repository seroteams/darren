# Phase 4 — docs/todo housekeeping

**Part of:** [PLAN.md](PLAN.md) · **Status:** ⬜

## Goal
The `docs/todo/` folder tells the truth: active means active, finished lives in `done/`.

## Changes
- Cross-check the ~12 active `docs/todo/` folders against STATUS.md and git history.
- Move genuinely finished plans to `docs/todo/done/` (with Carl's confirmation per folder — nothing closed silently).
- Update STATUS.md's parked/backlog table so the folder list and the table agree.

## Not in this phase
- No code changes.
- No closing of plans that are built-but-awaiting-QA (e.g. error-log Phase 3, mobile-responsive) — awaiting a walk is still active.

## Done when
- [ ] A short list of proposed moves was shown to Carl and he approved each.
- [ ] `docs/todo/` contains only genuinely open plans.
- [ ] Product owner has tested the scenario below and said go.

## Test scenarios — for the product owner
1. **The folder is honest** — look at the folder list in `docs/todo/`. For each folder, you should agree "yes, that's still open". ❌ Not OK if you spot something you signed off months ago still sitting there.
2. **Trackers agree** — STATUS.md's plan table mentions the same set of active plans as the folder list.
