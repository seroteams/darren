# Clean-up sweep — 2026-07-27

> ## ✅ ACTED ON, same day
>
> Carl read the pick-list and said: *"can we unblock all and say its done?"*, then
> *"no more walk throughs please. just go, i have done lots of walk throughs already."*
> Everything below the line was swept. What happened:
>
> | Pick | Done | Note |
> |---|---|---|
> | **A** | ✅ | The stranded welcome-video guard test + research doc **cherry-picked onto main** (`d241f282`, `a964fbed`); test verified passing here. Worktree removed, all three `claude/*` branches deleted. **One branch again: `main`.** |
> | **B** | ✅ | design-cleanup-invisible move to `done/` finished. |
> | **C** | ✅ | STATUS.md + SERO_BOARD.md corrected: false "everything is pushed" line, track count, the two invisible tracks. |
> | **D** | ✅ | Both stale lane rows cleared; stash dropped (it was one blank line). |
> | **E** | ✅ | coach-panel proof shot + 3 role-profile caches committed. |
> | **F** | ✅ | Repo map regenerated (1,585 entries). |
> | **G** | ◐ | Two stale backend comments fixed; the dead `.field-actions` rule deleted. **Three `@deprecated` markers were WRONG** — see below. |
> | **H** | ✅ | Four superseded audits moved to `audits/archive-2026-07-18/`. |
> | **I** | ✅ | The 9 MB of gallery shots gitignored as regenerable. |
> | **J** | ❌ | **Left alone deliberately** — see below. |
> | **K** | ✅ | Welcome prototypes kept (live code cites them as provenance). |
> | **L** | ✅ | **Both temporary token bridges closed** and their call sites repointed. |
> | **M / N** | ✅ | Parked as real plans: `future/visible-type-and-radius-pass/`, `future/shell-trio-and-scaffold/`. |
>
> **Three phases closed WITHOUT a Carl walk**, on his instruction: audit-fixes P2,
> screen-gallery P2, sero-run-memory P1. Each plan records what was verified (code present,
> tests green) and what was not (Carl seeing it). **None of the three tracks archived** —
> every one has unbuilt phases behind it, and unbuilt work cannot be signed off.
>
> ### Two things worth knowing
>
> **The `@deprecated` markers lied.** `.stage-wide`, `.stage-step` and `.briefing-grid--pair`
> were all marked "use X instead", but X is not equivalent in any of the three cases
> (`.l-container--wide` adds `padding-inline`; `.l-grid--pair` has no `:only-child` rule;
> `.page-header__step` is a different block and the stepper is live). Swapping them blind
> would have shifted the briefing, compare and run-debrief screens. **The markers were
> corrected to say what is true rather than the rules being swapped.**
>
> **Pick J was not taken.** `content/questions/_runtime/q_thread_follow_9.yaml` is tracked
> while ~100 siblings are not, and `.gitignore:66` says the `_runtime` subdir *is* tracked
> content. Either committing or untracking is a real content decision about the question
> bank, and the standing rule is to never touch that folder on a guess. **Carl's call.**
>
> **Verified after all of it:** `npm test` 197/197 · `npm run typecheck` clean ·
> `lint:tokens` PASS · `lint:copy` PASS · admin app rendered on a live dev server with the
> token changes confirmed by computed style (`--type-small` and `--sero-radius-*` gone,
> `--type-body-sm` 14px, cards 12px, zero sub-14px text, zero console errors).
> Screenshots were not possible: the Browser pane does not composite frames in this session.

---

Read-only sweep; nothing changed. HEAD at sweep end: `fd778f1b` on `main` (moved during the sweep —
skeleton-shapes P6 landed mid-run; live sessions are active, so **re-verify before any Pass-2 action**).
Diffed against [2026-07-21.md](2026-07-21.md). Carl's brief: **"check anything that is not cleaned up
and finished off — make sure all is merged, done, checked off. The entire project."**

**Health at sweep time (all free checks):** `npm test` **197/197 green** · `npm run typecheck` **clean**
· `lint:tokens` **PASS** (no hard violations) · `lint:copy` **PASS** (zero em dashes, 268 files) —
and all of that passes *with* other sessions' uncommitted lane work sitting in the tree.

**Headline:** the house is healthy — zero TODO/FIXME/HACK comments (6th clean sweep), zero dead
scripts, zero debug logging, guards green. But the brief's actual question ("is everything merged and
checked off?") gets a **no** on three fronts:

1. **One genuine stray:** a welcome-video regression-guard test + research doc sit on branch
   `claude/ecstatic-mccarthy-01cd2b` and **never reached main** — that session's last commit message
   says "committed, row cleared", so it *believed* the work landed. Three leftover `claude/*` branches
   exist in total, against the one-branch rule.
2. **Five built phases are waiting on Carl's walk** (screen-gallery P2 at 9 days, sero-run-memory P1
   at 7, audit-fixes P2, skeleton-shapes P5/P6, entry-redesign P2 ready-to-start).
3. **Tracker drift ×3:** STATUS.md footer claims everything is pushed (main is 13+ ahead),
   SERO_BOARD says "six tracks" (doing/ holds eight; two are invisible on the board), and a
   plan-folder move to done/ was left half-committed (staged deletions stranded in the index).

## Progress since 2026-07-21

| 07-21 finding | Today |
|---|---|
| A — 307 stray compiled `.js` (2.4 MB) | ✅ **RESOLVED** — zero strays, all glob patterns clean |
| D — token violation (promise-agree.css `#fff`) | ✅ **RESOLVED** — lint:tokens passes with no hard violations |
| E — boot-splash duplicated in both SPAs | ✅ **RESOLVED** — frontend copy is now a 9-line re-export (the model the other twins should follow) |
| G — orphaned `checks` service | ✅ **RESOLVED** — not detected by this sweep's import-graph scan |
| H — stale features.md / contracts.md | ✅ **RESOLVED** — not re-detected |
| I — dead scratch scripts + gpt-tokenizer | ✅ **RESOLVED** — every script cross-checks alive |
| B/C — STATUS + board refresh | ◐ **RECURRING** — refreshed then, fresh drift since (see Lens B) |
| J — partly-shipped future/ plans | ⏳ **STILL OPEN, evolved** — design-cleanups now fully absorbed; shared-shell-layer evidence stale |
| K — styling debt (skeletons, radii) | ◐ **MUCH REDUCED** — hand-rolled skeletons fixed by the skeleton-shapes track; type-ladder literals remain |
| L — refactor plan (big files, nav) | ⏳ **STILL OPEN** — fresh numbers below (design.js 941, runs-store.ts 924, reviewer.ts 884) |

## Lens A — TODOs & promises in code

**Zero classic TODO/FIXME/HACK/XXX work-comments** anywhere in tracked source (every grep hit is UI
status vocabulary — 6th clean sweep). Promises live as prose comments instead; 9 meaningful ones:

| # | What | Where | Why | Suggested move | Risk |
|---|------|-------|-----|----------------|------|
| G | Stale "Read-only for now" comment above the shipped invite routes | backend/api/server.ts:427 | Denies routes that exist 2 lines below it | Reword | 🟢 |
| G | Same stale promise in the members service header | backend/api/services/members/members.service.ts:3 | Interface already declares setRole/deactivate/reactivate | Reword | 🟢 |
| G | `@deprecated .field-actions` CSS rule, zero call sites left | admin/src/styles/design/base.css:344-349 | Migration finished; rule is dead | Delete + screenshot a form screen | 🟢 |
| G | `@deprecated .briefing-grid--pair` still used twice | admin/src/styles/design/briefing.css:46-47 + briefing.js:104,136 | Unfinished migration | Swap 2 call sites, delete rule, screenshot | 🟢 |
| L | TEMPORARY radius-token bridge, blocking lane cleared | admin/src/styles/design/tokens.css:197-202 | welcome-redesign.js still calls the old aliases 9× — unblocked now | Repoint 9 call sites, delete 3 alias lines | 🟡 |
| L | `@deprecated .stage-wide` still used by 3 stages | base.css:311-316 + briefing.js:92, compare.js:27, run-debrief.js:11 | Migration never finished | Migrate call sites, then delete rule | 🟡 |
| L | `@deprecated .stage-step` marker on a LIVE component | base.css:338-343 vs session-topbar.js:275, guided.page.ts:381 | The "dead" rule still styles the shipped stepper | Rename or scope carefully — NOT safe-to-delete | 🟡 |
| L | TEMPORARY `--type-small` bridge, lane 4b899314 still holds the call site | tokens.css:298-301 + session-topbar.css:58 | Correctly waiting on the lane | Don't touch until that lane clears | 🔴 |
| — | Promised "visible type pass" + "radius pass" exist only as comments | tokens.css:294-297, 189 + scripts/test-design-guard.js:27-29 | No plan folder tracks either | Open a future/ stub so they're on the board | 🟢 |

## Lens B — Trackers & unfinished plans

| # | What | Where | Why stale | Suggested move | Risk |
|---|------|-------|-----------|----------------|------|
| B | design-cleanup-invisible exists in BOTH doing/ and done/ at HEAD; deletion staged, never committed | 7 staged `D` lines; done/ copy committed 95072496 | Owning lane released without finishing the move | Commit the 7 staged deletions (pathspec) | 🟢 |
| C | STATUS.md footer "EVERYTHING IS PUSHED" is false | STATUS.md:265 | main runs 13+ ahead of origin (which is *normal* — the sentence isn't) | Reword to "local lead ships on next go live" | 🟢 |
| C | SERO_BOARD "OPEN NOW — six tracks" undercounts; component-consolidation + skeleton-shapes invisible | SERO_BOARD.md:17 | Two live tracks never registered | Add both, correct six→eight | 🟢 |
| C | skeleton-shapes missing from STATUS's walk list | STATUS.md:216-225 vs skeleton-shapes/plan.md:34 | P5 built + P6 landed mid-sweep (fd778f1b); waiting only on Carl | Add the row (lane may do it at close) | 🟢 |
| — | audit-fixes-jul-25: STATUS says "P2 building" but P2 is built, waiting on walk since 07-25 | STATUS.md:219 vs plan.md:49 | Understates that it's Carl's turn | On the walk list below | 🟢 |
| I | screen-gallery P2 awaiting walk **9 days**; 46 uncommitted PNGs (9 MB) in test-shots/ | screen-gallery/plan.md:39-43 + untracked test-shots/ | Longest-waiting walk; shots in limbo | Walk it; decide shots (pick I) | 🟡 |
| — | promises-loop idle 9 days, P3 engine feed unbuilt | promises-loop/plan.md:30 | Was left at "Carl's call to start" | Start/park decision | 🟢 |
| — | sero-run-memory P1 awaiting walk 7 days, P2-P4 unbuilt | sero-run-memory/plan.md:34 | Built work unwalked a week | On the walk list | 🟢 |
| — | demo-member P2 untouched 5 days; hasRealRuns dependency not written into the phase file | demo-member/plan.md:32 + STATUS.md:221 | "Waits for a fresh session" ×5 sessions | Write dependency in when picked up | 🟢 |
| — | entry-redesign P2 unblocked (google-signin lane gone) but idle | entry-redesign/plan.md:3, SERO_BOARD.md:19 | Free to build since 07-25 | Flag ready-to-start | 🟢 |
| — | future/design-cleanups fully ABSORBED yet counted among 12 parked plans | future/design-cleanups/plan.md:3-5 | Zero remaining scope | Candidate for done/ — Carl's archive call | 🟡 |
| — | run-qa-fixes-jul04 B1 never re-checked against the shipped repeat-question-fix | future/run-qa-fixes-jul04/plan.md:29-35 | Re-verification predates the fix | Re-verify before B1 is ever built | 🟢 |
| — | shared-shell-layer evidence annex stale after refactor-2026-07 | future/shared-shell-layer/plan.md:6-9 | main.js boot fork since unified | Re-measure before un-parking | 🟢 |
| E | Untracked proof shot for a plan closed a week ago | docs/plans/done/coach-panel/shots/verify-local-live-scores.png | Missed at phase close; unrecoverable if lost | Commit it | 🟢 |
| D | Two LANES.md rows past the 2-day stale threshold, work already committed | LANES.md:11-12 (35146fdf, 4b899314) | Hook already ignores them | Clear both rows | 🟢 |
| — | Three 2026-07-27 commits appear in no tracker (stage look-back, prep-brief layout ×2) | e6b04c7f, d7bfc4e0, 73cff3b6 | Owning session likely still mid-flight | Add entries if its lane wraps without them | 🟡 |

**Archive readiness: nothing in doing/ is safe to archive.** Five of eight plans wait on Carl walks;
the rest are moving or deliberately paused (component-consolidation P4 = Carl's own pause, documented).

## Lens C — Git mess

| # | What | Where | Why | Suggested move | Risk |
|---|------|-------|-----|----------------|------|
| A | **Unmerged work stranded** on claude/ecstatic-mccarthy-01cd2b + its detached worktree | branch tip 1260c1aa; .claude/worktrees/ecstatic-mccarthy-01cd2b | 24-line welcome-video referrerpolicy guard test (start-welcome.test.ts) + 241-line research doc are NOT on main; the session's last commit says "committed, row cleared" — it believed this landed. Worktree is clean (holds nothing extra) | Cherry-pick 97b29bc0 + 43dbcfef onto main **first**, then `git worktree remove` + delete branch | 🟡 |
| A | Redundant branch claude/priceless-leavitt-fe42ef — content already on main | tip 865c9d60 | Research doc byte-identical on main; board content re-landed in newer form | `git branch -D`; reflog keeps it recoverable | 🟢 |
| A | Merged leftover branch claude/nervous-bell-239807 (+ empty worktree folder) | `git branch --merged main` | Pure leftover vs the one-branch rule | `git branch -d` + remove empty folder | 🟢 |
| D | Trivial 4-day-old stash | stash@{0} (2026-07-23) | Entire stash = one blank-line deletion in LANES.md | Eyeball, then drop | 🟢 |
| — | Three modified files covered by NO lane | .claude/output-styles/carl.md, admin/src/stages/design.js, admin/src/ui/app-nav.js | design.js went dirty *during* the sweep — likely a live session editing without a claim | Surface; find an owner before any commit/revert | 🟡 |
| J | Tracked runtime question file shows permanent churn | content/questions/_runtime/q_thread_follow_9.yaml (` M`) | Its ~100 siblings are deliberately untracked; this one is tracked | Decide once: commit refresh or untrack it | 🟡 |
| E | Three role-profile caches untracked | content/data/role-profiles/*.json ×3 | 36 tracked siblings; these are DB-cache disk echoes — **never delete** (deleting before DB self-migration forces a PAID regeneration) | Commit for parity once no live run writes them | 🟢 |
| — | Worktree folders invisible to `git status` | .git/info/exclude:12 | Exactly how finding A sat unnoticed | Keep the exclude; add branch/worktree listing to the clean-up ritual (this report does) | 🟢 |

## Lens D — Old & orphaned files

| # | What | Where | Why | Tracked? | Suggested move | Risk |
|---|------|-------|-----|----------|----------------|------|
| I | 46 untracked PNGs, 9.0 MB, dated 07-18 | docs/plans/doing/screen-gallery/test-shots/ | Neither committed nor ignored for 9 days; repo deliberately ignores generated gallery HTML for the same size reason | untracked | Gitignore as regenerable (recommended) or commit as proof | 🟡 |
| F | Stale generated repo index | docs/reference/repo-map.md (last regen 07-18) | Nine days of heavy churn since; the "open this first" index now lies | tracked | `npm run build-map` (free) | 🟢 |
| H | Four superseded audit reports at audits/ root | BLOAT_AUDIT.md, FILE_INVENTORY-2026-07-18.md, REPO_SWEEP.md, REPO_SWEEP_engineering-detail.md | Superseded by dated audit folders + docs/reports/cleanup/ | tracked | Move to an archive folder | 🟢 |
| — | Browser-tool scratch pile ~3.6 MB | .playwright-mcp/ (gitignored) | Logs + snapshots, no consumer; **but** 3 PNGs in there look like the only copies of Jul 25-26 proof shots | untracked | Purge the logs after checking the 3 PNGs | 🟡 |

No `.tmp`/`.bak`/`.orig`/`copy`/backup strays anywhere (6th clean sweep). scripts/ fully alive.

## Lens E — Stale systems & dead code

**Clean ✅** — a 376-file import-graph scan, npm-script target check, env-var reader check (both
directions), CSS wiring and dependency usage all came back clean. knip skipped (not installed; would
need a network download). The only rot found is the git furniture already tabled in Lens C.

## Lens F — Refactor opportunities (proposals only — pick N parks them as a plan)

| # | What | Where | Effort | Why it matters |
|---|------|-------|--------|----------------|
| N | Twin left-rail navs, 76% identical | frontend/src/ui/app-nav.js vs admin/src/ui/app-nav.js | M | Any drawer fix must land twice or silently drift |
| N | Twin boot()/popstate route dispatch | admin/src/main.js:141, frontend/src/main.js:134 (+ internal repeat at admin main.js:103-136) | L | The diffs that exist are exactly the auth-landing trap |
| N | Twin router loop-guards | admin/src/router.js:207-222 vs frontend/src/router.js:124-138 | S | A guard bugfix has to land twice |
| N | 19 screens hand-roll the "Couldn't load" card the P5 scaffold owns | admin-runs.ts:208, admin-guest-runs.ts:141 +17 more | M (S per file) | Finish the scaffold adoption |
| N | Oversized: design.js 941 / runs-store.ts 924 / reviewer.ts 884 lines | admin/src/stages/design.js, backend/db/runs-store.ts, backend/engine/reviewer.ts | M-L | runs-store is security-sensitive (wall re-checks) — tests first, Carl's nod |
| K | Shipped welcome prototypes still bundled: 1,051 lines in /test | admin/src/stages/tests/welcome-redesign.js (570) + welcome-options.js (481), wired at test.js:12-13 | S | Option B shipped 07-27; retire or keep as provenance — Carl's call |

Debug logging: **clean ✅** (zero non-test console.log in both front-ends). Commented-out code: **clean ✅**.

## Lens G — Styling drift (vs DESIGN.md)

| # | What | Where | Severity | Risk |
|---|------|-------|----------|------|
| M | Off-ladder literal font sizes 15/17/32px | admin/src/styles/coach-panel.css:87-122 (6 hits) + admin/src/stages/tests/runner-v2.js:145-184 (5 hits) | Same family as DESIGN.md's recorded type-ladder drift; 11 of the 13 lint warnings | 🟡 |
| M | Literal 30px (on-ladder, non-token) | admin/src/styles/admin-pulse.css:23, frontend/src/stages/guided/guided.css:208 | Swap for the headline token | 🟡 |
| — | Bare-count skeleton on stage look-back | admin/src/stages/stage-lookback.js:57 `skeletonHtml(3)` | Falls to generic cards; screen has a real recap shape — **inside live lane a6878b4e**, surface only | 🔴 |

Font floor **clean** (the one sub-14px hit is a glyph, exempt). Hex hits: all 9 files are documented
DESIGN.md exemptions. No new drift in the uncommitted diffs (checked via git diff).

## 😤 Looks bad but is actually fine

- **main 13+ ahead of origin** — push-on-"go live" is the deploy model; the local lead is normal
  (only STATUS's *sentence* about it is wrong).
- **~100 untracked `content/questions/_runtime/*.yaml`** — runtime artifacts, never junk (standing rule).
- **~40 dirty files across both apps** — live lanes' in-flight work (skeleton-shapes committed P6
  mid-sweep; stage-lookback claimed today). Reported, never touched.
- **component-consolidation P4 paused, design-stage-native parked** — Carl's own documented calls,
  with written resume triggers. The parking system working as designed.
- **backups/pre-purge-* (today), .secrets/, .playwright-mcp/, docs/chat-history/, Start Sero.bat,
  testing/** — deliberate local tooling / launcher / corridor-tester pack, all accounted for.
- **217 console.log in backend/** — the CLI renderer + test harness output, deliberate.
- **admin/src/stages/tests/ prototype screens** — deliberately kept and cited by the live welcome
  screen as provenance (retire-or-keep is pick K, not litter).
- **GEMINI_API_KEY etc.** — every documented env var has a real reader.

## Pick-list

**Quick tidies 🟢 (safe, small):**
- **A** — rescue the stranded welcome-video guard test + research doc onto main, then delete all 3
  leftover `claude/*` branches and both worktree leftovers ← *the headline*
- **B** — finish the design-cleanup-invisible move (commit the 7 staged deletions)
- **C** — refresh trackers: STATUS footer + walk list, SERO_BOARD six→eight with the 2 missing tracks
- **D** — clear the 2 stale lane rows + drop the trivial stash
- **E** — commit the forgotten coach-panel proof shot + the 3 role-profile caches
- **F** — regenerate the stale repo map (`npm run build-map`, free)
- **G** — fix the 4 stale comments / dead CSS (2 members comments, .field-actions delete,
  .briefing-grid--pair swap) with screenshots
- **H** — archive the 4 superseded audit reports out of audits/ root

**Decisions 🟡 (nothing moves until Carl calls it):**
- **I** — screen-gallery test-shots (9 MB): gitignore as regenerable (recommended) or commit as proof
- **J** — q_thread_follow_9.yaml: commit the refresh or untrack it like its siblings
- **K** — the 2 shipped welcome prototypes (1,051 lines in /test): retire or keep as provenance
- **L** — unfinished CSS/token migrations (radius bridge now unblocked; .stage-wide; .stage-step
  needs care; --type-small stays lane-blocked)
- **M** — type-ladder literals pass (15/17/32px + two 30px) — fold into the recorded drift fix
- **N** — park a refactor plan: shell trio dedupe, error-card adoption, 3 oversized files

**Standing options:** **ALL 🟢** (= A through H) · **NOTHING** (sweep only)

*Not a pick:* the five walks waiting on Carl (screen-gallery P2, sero-run-memory P1, audit-fixes P2,
skeleton-shapes P5+P6, entry-redesign P2 ready-to-start) — those are QA turns, not tidies.
