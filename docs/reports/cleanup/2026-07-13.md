# Clean-up sweep — 2026-07-13

Read-only sweep; nothing changed. HEAD at sweep time: `61f31da8` (live sessions may commit — re-verify
before any Pass-2 action). Diffed against [2026-07-12.md](2026-07-12.md).

**Headline:** this session made ZERO edits, so the entire ~30-file working tree is other sessions'
in-flight work (onboarding→monthly-checkin refactor, finalize-briefing, role profile). All of it is
🔴 foreign — reported, never touched. Three of yesterday's git findings self-resolved.

## Progress since 2026-07-12

| Yesterday | Today |
|-----------|-------|
| D — `claude/eloquent-dijkstra` merged branch to delete | ✅ **RESOLVED** — branch gone |
| E — two `.claude/worktrees/` (unruffled-gauss, zealous-mclaren) to remove | ✅ **RESOLVED** — both gone |
| B — design-stage-native dormant, "park to future/" | ✅ **RESOLVED** — now in `docs/plans/future/design-stage-native/` |

## Lens A — TODOs & promises in code
**Clean ✅** — zero TODO / FIXME / HACK / XXX across admin/src, frontend/src, backend, scripts, shared. (STILL clean)

## Lens B — Trackers & unfinished work

| # | What | Where | Why stale | Suggested move | Risk |
|---|------|-------|-----------|----------------|------|
| A | STATUS.md + SERO_BOARD.md lag the worktrees: both say monthly-checkin "awaiting go on Phase 1" and admin-live "P2 next", but the worktrees are at **Phase 3 built** (`work/monthly-one-on-one`) and **P3** (`work/admin-serve`) | STATUS.md L34, SERO_BOARD.md L15-22 | Trackers refreshed yesterday, worktrees advanced overnight | Refresh both to the real worktree state | 🟢 |
| B | planner-grounding future plan's fix A ("drill, don't switch") appears already shipped by thread-follow (closed 07-11) — carried from yesterday, not yet reconciled | docs/plans/future/planner-grounding/plan.md | Partially superseded | Close or re-scope to still-open items (B, C) | 🟡 decision |

## Lens C — Git mess

| # | What | Where | Why stale | Suggested move | Risk |
|---|------|-------|-----------|----------------|------|
| C | Unmerged branch, ONE commit `fix(member-runs): type getRunsAboutMe…` (271143fc) — **confirmed NOT on main** (only branch containing it is itself) | `claude/laughing-williams-586fa7` | Orphaned fix, 1+ day old | Verify still needed → cherry-pick to main or delete | 🟡 decision |
| — | New skills added but never committed: `.claude/skills/clean-up/`, `.claude/skills/dependency-check/` — real repo tooling (the clean-up skill IS this run) | working tree (untracked) | Tooling landed, not saved to git | Commit just these two paths | 🟡 confirm not mid-edit |
| — | ~24 modified/deleted + finalize-briefing.ts/.test.ts + product-designer--junior.json — a live session's onboarding→monthly-checkin refactor + session-finalize feature | working tree | Not stale — in-flight | Untouched; their sessions commit | 🔴 foreign |
| — | `work/admin-serve` + `work/monthly-one-on-one` worktrees — active, match doing/ plans | worktrees | Not stale | Leave | ℹ️ |

No stashes.

## Lens D — Old & orphaned files

| # | What | Where | Why stale | Suggested move | Risk |
|---|------|-------|-----------|----------------|------|
| D | 1.3MB PNG referenced by nothing (carried from yesterday G) | docs/newrunnerdesign.png (tracked) | One-off design snapshot | Delete (git keeps history) or move to docs/archive/ | 🟡 confirm not a live ref |

Otherwise clean: no *.tmp/*.bak/*.orig/copies. The two "copy" grep hits (q_cta_copy_slip.yaml, copy-glossary.md) are legit.

## Lens E — Stale systems & dead code

| # | What | Where | Why stale | Suggested move | Risk |
|---|------|-------|-----------|----------------|------|
| E | `preparation.js` (242 lines) — ZERO importers; both apps route PREPARATION → frontend .ts. Only 3 string-label refs remain (carried from yesterday I) | admin/src/stages/preparation.js | Superseded | Delete + fix the 3 label refs (dev-badge.js:10, review-serialize.js:24, guide.js:91) | 🟡 confirm |
| F | 6 env vars read but documented nowhere: SERO_ENV, DEV_AUTOLOGIN, AI_MAX_CONCURRENCY, GUEST_RUNS_PER_DAY, OPENAI_MODEL, RUN_FILE_ECHO (carried from yesterday J) | backend | Undocumented knobs | Document in .env.example | 🟢 |

## Lens F — Refactor opportunities (proposals only)
Unchanged from yesterday — shell copies drift (K), two prep-brief implementations (L, resolves with E), scattered console.logs. **Report only**, no action inside a clean-up.

## Lens G — Styling drift (vs DESIGN.md)

| # | What | Where | Why drift | Suggested move | Risk |
|---|------|-------|-----------|----------------|------|
| — | tasks.js hardcoded palette + 11px sync-dot (yesterday M) — **file is live-session-modified right now** | admin/src/stages/tasks.js | Tokens + 14px floor | Not now → park until its session lands | 🔴 not now |
| G | universe.ts (~73 inline selectors + hexes) + meeting-arcs.js (~54) — no feature CSS file (carried from yesterday N) | admin/src/stages/universe.ts, meeting-arcs.js | Feature CSS not in own file | Extract to feature CSS or declare exemption | 🟡 |

Skeletons clean ✅ · design.css manifest clean ✅.

## 😤 Looks bad but is actually fine
- `content/questions/_runtime/*.yaml` (55+ untracked) — runtime question-bank artifacts, NEVER junk (standing rule).
- The ~30-file dirty working tree — live parallel sessions mid-refactor, not mess.
- `.claude/skills/clean-up` + `dependency-check` untracked — real tooling, just not committed yet (see finding above).
- `docs/chat-history/` + `scripts/chat-log.py` — gitignored local tooling, deliberate.
- Deleted onboarding files (type.ts, batch scenarios, future/monthly-one-on-one/*) — an in-flight session removing the onboarding 1:1 type; expected, not loss.

## Pick-list
- **A** — refresh STATUS.md + SERO_BOARD.md to real worktree state 🟢
- **B** — reconcile planner-grounding future plan 🟡
- **C** — verify + resolve laughing-williams branch (cherry-pick or delete) 🟡
- **D** — remove/archive docs/newrunnerdesign.png 🟡
- **E** — delete preparation.js + fix 3 label refs 🟡
- **F** — document the 6 env vars in .env.example 🟢
- **G** — extract universe.ts / meeting-arcs.js inline CSS 🟡
- **SKILLS** — commit the two untracked skill folders 🟡
- **ALL 🟢** — do A + F (both safe)
- **NOTHING** — sweep only

## Pass 2 outcome (Carl picked ALL 🟢, 2026-07-13)
Re-ran `git status` before touching anything (gotcha rule) — **both green items were already handled by live
sessions, so nothing was edited or committed:**
- **A (trackers)** — STATUS.md + SERO_BOARD.md already carry an **uncommitted 2026-07-13 refresh** from the
  monthly-checkin session (P1 green-lit block, updated Now/Next, today's last-updated line). Editing or committing
  would have swept their work — hands off (safe-commit). ✅ resolved by owner.
- **F (env vars)** — all 6 already documented in `.env.example` (modified Jul 12, lines 49-55). ✅ already done.

Net: zero changes this pass. B/C/D/E/G/SKILLS remain open for a future sweep.
