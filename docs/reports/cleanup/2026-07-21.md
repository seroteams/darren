# Clean-up sweep — 2026-07-21

Read-only sweep; nothing changed. HEAD at sweep end: `3068fddc` on `main` (moved during the sweep —
live sessions are active; re-verify before any Pass-2 action). Diffed against
[2026-07-17.md](2026-07-17.md). Carl's brief: **"using the map, do a clean-up — anything we can get
rid of."** Swept top-down from the repo map ([docs/reference/structure.md](../../reference/structure.md)),
which I refreshed at the start of this session.

**Headline:** the house is genuinely tidy — **13 of the 07-17 pick-list's items are now RESOLVED**
(board tombstone, root strays, org-data repo, orphan worktree, old logs, autostash, benchmark cluster,
shipped /test mocks, rename handover, stream-helper debug line, and more). Code hygiene stays excellent:
zero TODOs (5th clean sweep), zero broken npm scripts, zero dead env vars, one lone token violation.
The **one big new thing** is a pile of **307 stray compiled `.js` files (2.4 MB)** dumped across
`backend/` by an accidental compile on 2026-07-20 21:54 — pure litter, regenerable, nothing uses it.

## Progress since 2026-07-17

| 07-17 finding | Today |
|---|---|
| B — docs/board.md tombstone | ✅ **RESOLVED** — gone |
| C — RENDER_SETUP.md + ONE-ON-ONE-RUNNER-CONCEPT.md at root | ✅ **RESOLVED** — gone |
| F — .env.example GEMINI_MODEL dead line | ✅ **RESOLVED** — removed |
| H — org-data.repo.ts + test | ✅ **RESOLVED** — deleted |
| I — build-reviews.js + seed-manager.ts | ✅ **RESOLVED** — gone |
| J — benchmark 3-file cluster | ✅ **RESOLVED** — gone |
| K — superseded 07-15 autostash | ✅ **RESOLVED** — stash list empty |
| L — shipped /test mocks (setup-redesign, live-pulse) | ✅ **RESOLVED** — only promises-loop mock remains (keep) |
| M — orphan worktree ../serolocal-admin-serve | ✅ **RESOLVED** — gone |
| O — stream-helper.ts debug line | ✅ **RESOLVED** — removed |
| P — logs/may + June baseline strays | ✅ **RESOLVED** — gone (logs/ = 160 MB, all current July) |
| S — rename darren→serolocal handover doc | ✅ **RESOLVED** — gone |
| A — `_runtime/**` git-status noise | ◐ **STILL OPEN** — now 34 untracked (was 213); these are authored content, not junk — QoL only |
| N — styling drift | ⏳ **STILL OPEN** — much reduced; lint:tokens now 1 violation (was epidemic). Fresh detail below |
| T — refactor proposals (fork drift, big files) | ⏳ **STILL OPEN** — fresh numbers below |

## Lens A — TODOs & promises in code
**Clean ✅** — zero TODO / FIXME / HACK / XXX in tracked source or docs (verified multiple greps). The
only hits repo-wide are skill definitions, past reports, and `audits/` that quote the words. 5th clean sweep.

## Lens C — Git mess
**Clean ✅** — one branch (`main`), no worktrees, no stashes. Working tree churns with live-session work
(345 untracked, 8 modified tracked) — all foreign/in-flight, **reported not touched**. See Lens D for the
litter subset.

## Lens D — Old & orphaned files (the headline)

| # | What | Where | Why | Tracked? | Suggested move | Risk |
|---|------|-------|-----|----------|----------------|------|
| **A** | **307 stray compiled `.js` files, 2.4 MB** — every one has a `.ts`/`.mts` sibling; all stamped **2026-07-20 21:54** (single accidental `tsc` emit) | backend/api (158), backend/engine (109), backend/db (20), backend/shared (5), scripts/lib (3), 11 loose scripts, evals/trust-checks.js, backend/cli.js | App runs `.ts` directly (`node backend/api/server.ts`); tsconfig is `noEmit`; these `.js` were deliberately deleted during the TS conversion and got regenerated by accident. Nothing imports them | untracked — regenerable from `.ts` | Delete the 307 `.js` (one `tsc` re-emits if ever needed). One check: no live backend build depends on them (it doesn't) | 🟢 |
| — | Stray untracked docs: coach-panel verify screenshot, a 2026-07-07 night-test report in archive/, a compass research artifact | docs/plans/done/coach-panel/shots/, docs/reports/archive/, docs/research/ | Loose keep-or-commit files, low value | untracked | Commit the worth-keeping ones or leave | 🟢 low |

No `.tmp`/`.bak`/`.orig`/backup/scratch strays anywhere (5th clean sweep).

## Lens E — Stale systems & dead code

| # | What | Where | Evidence | Suggested move | Risk |
|---|------|-------|----------|----------------|------|
| **G** | Orphaned `checks` service — backend half of a shelved "free checks" feature, kept alive only by its own test | backend/api/services/checks/checks.service.ts:59,64,96 + its test | grep `runFreeCheck\|listChecks\|resolveCheck` → only the file + test; the route, controller, and shared/api.js wrapper are all deleted | Decision: resume the UI button or retire service + test | 🟡 |
| **I** | Dead/scratch one-off scripts (no npm/launch/code consumer) + one dep | scripts/check-role-profile-injection.js, scripts/plan-turn-size-report.js (sole `gpt-tokenizer` user — package.json:51), scripts/focus-example.js, scripts/promote-candidates.js | grep per script → 0 tracked consumers; most are documented **manual** tools | Decision: retire opportunistically or keep as manual tools | 🟡 |
| — | knip ran but is **unreliable here** (no config; flags the .js litter + every test as "unused") | — | only `gpt-tokenizer` survived filtering | Informational — don't act on raw knip | 🟢 |

npm scripts: all 17 targets exist ✅. Env vars: all documented vars are read ✅. seed/backfill/ops scripts
are consumer-less **by design** (manual migration tools) — not dead.

## Lens B — Trackers & unfinished work

| # | What | Where | Why stale | Suggested move | Risk |
|---|------|-------|-----------|----------------|------|
| **B** | STATUS.md drift: still shows `arc-evidence-fixes` building (it's **done**, moved to done/, all 3 phases green-lit 07-20); screen-gallery P2 shows old "demo data" scope (redefined + built, awaiting walk); `sero-run-memory` missing entirely; broken promises-loop link points to future/ | STATUS.md:53,59,61,75 (+ table 56-61) | Tracks born in other sessions never registered | Refresh STATUS.md | 🟡 |
| **C** | SERO_BOARD.md drift: says screen-gallery is "the ONLY track in doing/" (there are 3); test count "153/153" (current suites report 164/164); repo-state stamped 07-18 | SERO_BOARD.md:18,274,265 | Board not updated as work landed | Refresh the board | 🟡 |
| **J** | future/ plans partly overtaken by shipped work: planner-grounding (Scope A shipped via thread-follow), briefing-readability-p0 (P2 done by design-system side-effect), shared-shell-layer (folded into shared-folder-split) | docs/plans/future/{planner-grounding,briefing-readability-p0,shared-shell-layer}/ | Reality moved past the plans | Re-scope (not archive) when next touched | 🟡 |

**Archive readiness:** no `doing/` folder is safe to auto-archive. screen-gallery (P2 awaiting walk),
promises-loop (P3 engine feed open; plan banner says PARKED — arguably belongs in future/), sero-run-memory
(Phase 1 built, awaiting QA — and **tracked nowhere**, folded into pick B). 🔴 don't touch sero-run-memory's files — live.

## Lens F — Refactor opportunities (proposals only)

| # | What | Where | Effort | Why it matters |
|---|------|-------|--------|----------------|
| **E** | `boot-splash.js` byte-identical in both SPAs (155 lines, 0-line diff) + brandmark `<svg>` copy-pasted 5× | admin/src + frontend/src; app-nav.js ×2, session-topbar.js, orb.js, recap-pdf.ts | S | A tweak lands once, not 5×. Quick wins |
| **L** | Oversized stage files: briefing.js 860, design.js 767 (the design sheet — size-only), questioning.js 661, preparation-brief.ts 603; watchlist guide.js 595 / intake.js 556. Plus divergent admin↔frontend nav (266-line diff) and no canonical `.btn--primary` (reinvented per feature) | backend + admin/frontend | M–L | Hardest files to navigate; button drift. Park as a future refactor plan |

Debug logging: clean ✅ (zero console.log in both front-ends). Commented-out code: clean ✅.

## Lens G — Styling drift (vs DESIGN.md)

| # | What | Where | Severity | Risk |
|---|------|-------|----------|------|
| **D** | **Token guard: 1 violation** — `color:#fff` should be a token | admin/src/styles/design/promise-agree.css:42 | clear, tiny | 🟢 |
| **K** | Styling-debt pass: off-token border-radii scattered (~17 CSS hits — tokens are 4/12px); 8 stages embed feature CSS in JS `<style>` blocks instead of own `.css`; hand-rolled "Loading…" instead of `createSkeleton()` (guided.page.ts, member-home.js, members.ts, person-detail.ts); recap-pdf.ts hardcodes a 16-colour palette (+ untokened `#cccccc`) | see cited files | medium (M effort) | 🟡 |

**Font floor: clean ✅** — no sub-14px user-facing text. **One-blue-action:** no single-view violation
(the real risk is the missing canonical primary button — pick L). lint:tokens epidemic from 07-17 is
**largely fixed** — down to the one #fff above.

## 😤 Looks bad but is actually fine
- **345 untracked / 8 modified tracked files** — overwhelmingly **live-session work** (a big backend/api
  TS rebuild + nav re-org + others). Foreign, in-flight — reported, never touched.
- **34 untracked `content/questions/_runtime/*.yaml`** — authored question-bank content, NEVER junk (standing rule).
- **logs/ 160 MB** — all current-month July runs; the archive-old-months pattern handles it later.
- **gitignored local tooling** — docs/chat-history/, scripts/chat-log.py, .playwright-mcp/ — deliberate.
- **skills-lock.json at root** — legit lockfile, not clutter.
- **HEAD moving during the sweep** — live sessions doing their job.

## Pick-list

**Quick tidies 🟢 (safe, small):**
- **A** — sweep the 307 compiled `.js` litter (2.4 MB; regenerable, nothing uses it) ← the headline
- **B** — refresh STATUS.md (arc-evidence-fixes done, screen-gallery P2 scope, add sero-run-memory, fix promises-loop link)
- **C** — refresh SERO_BOARD.md (list the 3 doing/ tracks, 153→164 tests, re-stamp repo-state)
- **D** — fix the one token violation (promise-agree.css:42 `#fff` → `var(--color-surface)`)
- **E** — dedupe boot-splash.js + extract the brandmark SVG (S refactor)

**Decisions 🟡 (nothing moves until Carl calls it):**
- **G** — orphaned `checks` service: resume the UI or retire service + test
- **H** — stale reference docs `features.md` + `contracts.md` (still describe the deleted `src/`+CLI layout — 24 + 8 dead paths): rewrite to the TS layout or move to docs/archive/ as a frozen snapshot
- **I** — dead/scratch one-off scripts (+ `gpt-tokenizer` dep): retire or keep as manual tools
- **J** — re-scope the 3 partly-shipped future/ plans (planner-grounding, briefing-readability-p0, shared-shell-layer)
- **K** — styling-debt pass (off-token radii, CSS-in-JS extraction, hand-rolled skeletons) — M effort
- **L** — park a refactor plan (oversized files + divergent nav + canonical `.btn--primary`)

**Standing options:** **ALL 🟢** (= A+B+C+D+E) · **NOTHING** (sweep only)
