# Clean-up sweep — 2026-07-17

Read-only sweep; nothing changed. HEAD at sweep END: `6a890f78` on `main` (moved twice DURING the
sweep — a live session closed stream-hang-fix and deployed; re-verify before any Pass-2 action).
Diffed against [2026-07-15.md](2026-07-15.md). Carl's brief: **full audit — every file, what's
been done, what's not needed.**

**Headline:** code hygiene is excellent and getting better — zero TODOs (4th clean sweep running),
zero debug logging in both front-ends, zero commented-out code, zero junk strays, all npm scripts
and env flags live, and 8 of the 07-15 report's 16 findings are now RESOLVED. What's actually
untidy is **paperwork, not code**: SERO_BOARD.md lists none of the 7 open tracks, STATUS.md is
missing three of them, one plan file (admin-live-deploy) claims its work is stranded on a branch
that was in fact merged and deployed on 07-14, and the same handful of 🟡 deletions flagged on
07-15 are still sitting there untouched.

## Progress since 2026-07-15

| 07-15 finding | Today |
|---|---|
| B — commit clean-up + dependency-check skills | ✅ **RESOLVED** — both tracked |
| F — zip + delete logs/june (138 MB) | ✅ **RESOLVED** — gone |
| G — limbo testing/ dir | ✅ **RESOLVED** — tracked (test-plan, tester-pack, results/) |
| H — dead preparation.js + 3 stale refs | ✅ **RESOLVED** — deleted |
| K — orphaned member-runs.css | ✅ **RESOLVED** — imported by runs.ts:13 + member-home.js:10 |
| L — old monthly-checkin prototype | ✅ **RESOLVED** — deleted (2 stale comment refs remain, pick G) |
| P — planner-grounding / shared-shell reconcile | ✅ **RESOLVED** — 07-15 relevance audit annotated both |
| main ↔ work/monthly-checkin divergence | ✅ **RESOLVED** — single branch `main`, no local strays |
| A — `_runtime/**` gitignore rule | ⏳ **STILL OPEN** — now 213 noise lines (was ~90) |
| C — docs/board.md tombstone | ⏳ **STILL OPEN** |
| D — runner-concept pair | ◐ **HALF** — the 1.3 MB png is gone; the .md still sits at docs/ root |
| E — RENDER_SETUP.md at root | ⏳ **STILL OPEN** |
| I — org-data.repo.ts + test | ⏳ **STILL OPEN** |
| J — build-reviews.js / seed-manager.ts / benchmark cluster | ⏳ **STILL OPEN** |
| M — hex-fallback styling drift | ⏳ **STILL OPEN** (fresh detail below) |
| O — rename darren→serolocal | ⏳ **STILL OPEN** (scheduled "tonight" on 07-08) |
| N — big-refactor proposals | ⏳ **STILL OPEN** (updated numbers below) |

## Lens A — TODOs & promises in code
**Clean ✅** — zero TODO / FIXME / HACK / XXX across admin/, frontend/, backend/, shared/, scripts/
(verified three grep ways). All "todo" hits are task-board domain vocabulary. 4th clean sweep running.

## Lens B — Trackers & unfinished work

| # | What | Where | Why stale | Suggested move | Risk |
|---|------|-------|-----------|----------------|------|
| D | STATUS.md missing three active tracks (personal-data-security — a SECURITY track; team-page-redesign — P3 live 07-17; wrap-up-exit — P1 building 07-17); briefing-before-during-after link points to doing/ but folder is in done/; footer lags ui-look-and-feel P3 | STATUS.md L40, L363 | Tracks born in other sessions never registered | Refresh STATUS.md | 🟢 |
| E | SERO_BOARD lists NONE of the 7 open doing/ tracks; "Now" block leads with axis-memory (closed 07-16); test count "127/127" ×2 (current suites report ~154); repo-state block "audited 2026-06-29" (~3 weeks) | SERO_BOARD.md L15, L201, L262, L254 | Board rule "update the moment work lands" not honoured | Refresh the board | 🟢 |
| R | admin-live-deploy plan claims P2/P3 "stranded on work/admin-serve, merge BLOCKED" — but merge commit `ffc5165d` landed on main 2026-07-14 and the branch is deleted; the worktree folder `../serolocal-admin-serve` survives as a 1 MB orphan (unregistered, node_modules stub only) | docs/plans/doing/admin-live-deploy/plan.md L52-64 | Plan lags reality by 3 days | Decision: confirm P2/P3 look right live → refresh plan, delete orphan folder, decide P4-P6 | 🟡 |
| Q | promises-loop dormant 5 days: P1 green-lit 07-12, P2 (zero-resurface + close-out) and P3 (Q1 feed + surfacing) never started; the orphaned outcomeCheck consumer is P2's job | docs/plans/doing/promises-loop/ | Half-built loop, quietly cooling | Decision: un-park or formally park. **Not safe to archive — P2/P3 open** | 🟡 |

Other doing/ tracks are alive and honest: ui-look-and-feel (3 phases await Carl QA), team-page-redesign
(only P2 walk outstanding), members-page (final walk outstanding), personal-data-security (P2 built
uncommitted, P3 interim), wrap-up-exit (P1 building). **None is archive-safe.** stream-hang-fix was
closed to done/ by its owning session DURING this sweep — already handled, not a finding.
future/ folder: all plans carry accurate 07-15 relevance-audit verdicts; nothing wholesale superseded.

## Lens C — Git mess

| # | What | Where | Why stale | Suggested move | Risk |
|---|------|-------|-----------|----------------|------|
| A | 213 untracked `content/questions/_runtime/*.yaml` drown every git status; the one-line gitignore fix has been "parked" since 07-12 | .gitignore | Agreed, never landed | Land the rule (hides from status, deletes nothing) | 🟢 |
| K | Autostash from 2026-07-15 18:19 (20+ files: add-person modal, admin-pulse, error-log…) still in the stash list; spot-checks show its content landed via commits `4b54af3f`, `a84fb9ef` etc. (add-person-modal.css exists, pulse changes committed) | `stash@{0}` | Rebase leftovers, superseded | Drop the stash after a final glance | 🟡 |
| M | Orphan folder `../serolocal-admin-serve` (1 MB, only a node_modules stub) — not a registered worktree, branch gone, work merged | ../serolocal-admin-serve | Worktree cleaned up halfway | Delete the folder (part of pick R's refresh) | 🟡 |

Working tree at sweep end: clean apart from the _runtime YAMLs. One branch (`main`), no local
stale branches. Remote has 5 old `claude/*` branches on origin — untouched (remote, harmless).

## Lens D — Old & orphaned files

| # | What | Where | Why stale | Tracked? | Suggested move | Risk |
|---|------|-------|-----------|----------|----------------|------|
| B | Tombstone stub "The board — moved… retired" | docs/board.md | Dead pointer since 07-13 | tracked | Delete | 🟢 |
| C | Deploy how-to at repo root + orphan concept doc at docs/ root (its png already archived) | RENDER_SETUP.md, docs/ONE-ON-ONE-RUNNER-CONCEPT.md | Root clutter | tracked | Move to docs/reference/ + docs/archive/ | 🟢 |
| I | Scripts with no caller anywhere (flagged 07-15, unactioned): build-reviews.js (regenerates static review pages), seed-manager.ts (siblings are referenced; this one by nothing) | scripts/ | No live consumer | tracked | Delete (git keeps history) | 🟡 |
| J | Manual paid benchmark cluster: benchmark.js + verdict-template.html + suggest-judge.js — unwired to npm scripts; eval-judge.js + lint-bank.js it imports ARE shared/live (keep those) | scripts/ | Phase-0 tool, unused since | tracked | Carl's call: keep as manual tool or retire the 3-file cluster | 🟡 |
| P | logs/may (1.1 MB, May 24-25) + June baseline strays (logs/index.html, _baseline_dir.txt, _baseline_sweep_current.log) | logs/ | ~2 months old | untracked — **permanent delete** | Prune if those runs are done with | 🟡 |
| S | Rename darren→serolocal scheduled "tonight 2026-07-08" — 9 days on, folder still `darren`; handover doc misleading | docs/rename-serolocal-handover.md | Stalled decision | tracked | Decision: re-schedule (script ready) or shelve | 🟡 |

logs/july = 139 MB but is the CURRENT month — not stale. `.playwright-mcp/` dumps are gitignored
scratch — fine. Zero .tmp/.bak/.orig/copy strays anywhere (4th sweep running).

## Lens E — Stale systems & dead code

| # | What | Where | Evidence | Suggested move | Risk |
|---|------|-------|----------|----------------|------|
| H | Phase-006 scaffold repo whose only importer is its own test; org-fence lives in runs.service | backend/api/services/auth/org-data.repo.ts + org-data.test.ts | grep: zero production refs (2nd sweep confirming) | Delete both | 🟡 |
| L | /test gallery mocks whose features SHIPPED: setup-redesign (shipped `16180040`), live-pulse (Pulse merged `ffc5165d` 07-14). promises-loop mock still matches an open track — keep it | admin/src/stages/test.js:6-8 | shipped commits vs gallery | Retire the two shipped mocks (or keep as history — Carl's call) | 🟡 |
| G | Three stale comments lying about the codebase: frontend/src/main.js:51 claims "admin keeps its own copy" of preparation (false — admin imports the frontend file); guided.page.ts:6 + guided-icons.ts:2 cite the deleted monthly-checkin.js | see paths | files verified | Fix the 3 comment lines | 🟢 |
| F | .env.example lists GEMINI_MODEL — no code reads it (gemini-client.ts reads only GEMINI_API_KEY); conversely ERROR_LOG_RETENTION_DAYS is read (purge-error-logs.js:10) but absent from .env.example | .env.example:9 | grep | Drop the dead line, add the missing one | 🟢 |
| O | One stray debug line: per-event subscriber-count log gated on `terminal` | backend/api/handlers/stream-helper.ts:139 | only real leftover-debug candidate in the repo | Drop or put behind a debug flag | 🟡 |
| — | CI gap (observation): typecheck:admin / typecheck:customer defined but CI runs only root typecheck | package.json:23-24 vs .github/workflows/ci.yml:31-38 | config read | Note only — wire into CI when convenient | 🟡 |

Everything else checked has a live consumer in at least one app (start-core, intake-firstrun,
guided/*, review-html, agenda, all seed siblings, all ~45 auto-discovered test-*.js). All npm
scripts resolve. All env flags read. knip not installed — skipped (would download).

## Lens F — Refactor opportunities (proposals only)

| # | What | Where | Effort | Why it matters |
|---|------|-------|--------|----------------|
| T1 | Shell fork drift: router.js (80 shared lines, 219 vs 123), main.js (234 shared, 491 vs 381), app-nav.js (142 shared, 354 vs 219) each copied admin→frontend and diverging; shared/ exists (api.js, sse.js) but these never moved in | admin/src + frontend/src | M each | Route/nav/boot bugs fixed once, not twice |
| T2 | 800+ line files: reviewer.ts 935, runs-store.ts 852, run-history.ts 801; next tier sessions.service 777, design.js 754, briefing.js 749, role-profile.ts 748, onepage.js 732, server.ts 727, golden-checks.ts 724 | backend + admin | M-L | Natural split points exist; hardest files to navigate |

Debug logging: clean ✅ (zero console.log in both front-ends; backend console output is CLI-by-design).
Commented-out code: clean ✅ (high `//` counts are prose documentation).

## Lens G — Styling drift (vs DESIGN.md)

| # | What | Where | Severity | Risk |
|---|------|-------|----------|------|
| N | **Font-floor breaches (14px rule):** guided.css has 8× `font-size:13px` + 1× 11px on user-facing text; guided.page.ts:324 inline 13px error text | frontend/src/stages/guided/ | clear | 🟡 |
| N | **Bare hex instead of tokens:** guided.css ~13 bare `#fff` lines; member-home.js styles a whole feature inline (incl. `#a3372c` — not a token; coral-800 is `#ac1608`) with no member-home.css; session-topbar.css hardcodes the accent `#5aa9e6` + non-token `#24445c`; tasks.js KB_LANE_COLORS = 8-colour non-Sero palette | see files | clear | 🟡 |
| N | **Wrong-token fallbacks epidemic:** `var(--color-ink,#102d42)` (token is `#1f2a37`), `var(--sero-coral-800,#a3372c)` (token `#ac1608`), three different fallbacks for --color-ink-mute — across guided.css, tasks.js, personas.js, guide.js, member-home.js, finish-feedback-modal.css | see files | borderline | 🟡 |

One-blue-action rule: no violation found. Skeletons: clean ✅ — all loaders use createSkeleton();
only canonical skeleton CSS exists. preparation.css is the model citizen (guarded by a no-hex test)
— the drift pattern is everything that doesn't have that guard.

## 😤 Looks bad but is actually fine
- 213 untracked `content/questions/_runtime/*.yaml` — runtime question-bank artifacts, NEVER junk
  (standing rule). Pick A hides them from git status without touching a single file.
- logs/july at 139 MB — the CURRENT month's run logs; the archive-old-months pattern handles it later.
- `.playwright-mcp/` console/page dumps — gitignored browser-automation scratch, deliberate.
- docs/chat-history/ + scripts/chat-log.py — gitignored local tooling, deliberate.
- ~45 scripts/test-*.js — real suites auto-discovered by run-tests.js.
- Backend console.log volume — interactive CLI stages + tagged startup logs, not leftover debug.
- High comment density in server.ts / run-history.ts / main.js — prose documentation, not dead code.
- LIVE_DATABASE_URL "read by nothing" — deploy-time alias by design. SERO_ENV — legacy-named but live.
- 5 old `claude/*` branches on origin — remote-only, harmless, no local counterparts.
- HEAD moving during the sweep (stream-hang close + deploy) — a live session doing its job.

## Pick-list

**Quick tidies 🟢 (safe, reversible, small):**
- **A** — land the `_runtime/**` gitignore rule (kills 213 git-status noise lines, deletes nothing)
- **B** — delete the docs/board.md tombstone
- **C** — move RENDER_SETUP.md → docs/reference/ and ONE-ON-ONE-RUNNER-CONCEPT.md → docs/archive/
- **D** — refresh STATUS.md (add the 3 missing tracks, fix the done/ link + footer)
- **E** — refresh SERO_BOARD.md (list the 7 open tracks, fix 127/127, re-stamp repo-state)
- **F** — .env.example: drop GEMINI_MODEL, add ERROR_LOG_RETENTION_DAYS
- **G** — fix the 3 stale comments (false "own copy" claim + 2 refs to a deleted file)

**Bigger tidies 🟡 (one check each):**
- **H** — delete org-data.repo.ts + its test
- **I** — delete build-reviews.js + seed-manager.ts
- **J** — benchmark 3-file cluster: keep or retire (Carl's call)
- **K** — drop the superseded 07-15 autostash
- **L** — retire the two shipped /test mocks (setup-redesign, live-pulse)
- **N** — styling-debt pass: guided.css font floor + bare #fff, member-home.css extraction, session-topbar accent, wrong fallbacks (M effort)
- **O** — drop the stream-helper.ts:139 debug line
- **P** — prune logs/may + June baseline strays (untracked = permanent; ~1.2 MB)

**Decisions 🟡 (nothing moves until Carl calls it):**
- **Q** — promises-loop: un-park (finish P2/P3) or formally park. Not archive-safe.
- **R** — admin-live-deploy: confirm Pulse looks right live → refresh the stale plan, delete the orphan folder, decide P4-P6. Not archive-safe.
- **S** — rename darren→serolocal: re-schedule or shelve the handover doc
- **T** — park the two refactor proposals (shell fork drift, 800-line files) as a future plan

**Standing options:** **ALL 🟢** (= A+B+C+D+E+F+G) · **NOTHING** (sweep only)
