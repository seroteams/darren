# Clean-up sweep — 2026-07-15

Read-only sweep; nothing changed. HEAD at sweep time: `6992e456` on `work/monthly-checkin` (live
sessions may commit — re-verify before any Pass-2 action). Diffed against [2026-07-13.md](2026-07-13.md).
Carl's brief this time: **whole-repo — combine, remove, simplify; the repo feels long, untidy, not
categorised enough.** So this sweep leans extra hard on layout/categorisation.

**Headline:** the repo has almost NO junk (zero TODOs, zero debug logs, zero commented-out code,
zero .bak/.tmp strays — three sweeps in a row). The "untidy" feeling comes from four real sources:
**(1)** ~90 untracked runtime YAMLs polluting every `git status` (the parked gitignore rule),
**(2)** 244 MB of old local run logs on disk, **(3)** a handful of loose one-off files at root and
docs/ top level, and **(4)** ~35 files of live parallel-session work in flight, which reads as mess
but is actually other sessions mid-task.

## Progress since 2026-07-13

| 07-13 finding | Today |
|---|---|
| C — orphaned `claude/laughing-williams` branch | ✅ **RESOLVED** — branch gone |
| G — universe.ts / meeting-arcs inline CSS | ✅ **RESOLVED** — DESIGN.md §6 now declares these exempt |
| A (trackers) + F (env vars) | ✅ resolved in the 07-13 Pass 2 (by owners) |
| B — planner-grounding reconcile | ⏳ **STILL OPEN** (pick P below) |
| D — newrunnerdesign.png (1.3 MB, unused) | ⏳ **STILL OPEN** (pick D below) |
| E — dead preparation.js + 3 stale refs | ⏳ **STILL OPEN** (pick H below) |
| SKILLS — two uncommitted skill folders | ⏳ **STILL OPEN** (pick B below) |

## Lens A — TODOs & promises in code
**Clean ✅** — zero TODO / FIXME / HACK / XXX across admin/ (104 files), frontend/ (24), backend/ (275),
shared/, scripts/. All "todo" hits are the Tasks-board's own domain vocabulary. (3rd clean sweep running.)

## Lens B — Trackers & unfinished work

| # | What | Where | Why stale | Suggested move | Risk |
|---|------|-------|-----------|----------------|------|
| — | SERO_BOARD says "5 in-flight" but one row is "✅ CLOSED 2026-07-15" and missing from Recently-shipped; repo-state block audited 2026-06-29 (~16 days old); test count 127/127 vs 131/131 elsewhere | SERO_BOARD.md §1, repo-state, test-status | Board over-counts + stale blocks | **NOT NOW** — STATUS.md + SERO_BOARD.md are STAGED by a live session; refresh once it commits | 🔴 foreign |
| — | manager-workspace-prototype "closed + moved to done/" per both trackers, but the done/ folder AND its prototype code are uncommitted (staged/untracked) | docs/plans/done/manager-workspace-prototype/, admin/src/stages/tests/manager-workspace.prototype.* | "Closed" claim ahead of git | **NOT NOW** — owning session should commit its own close | 🔴 foreign |
| — | monthly-one-on-one plan says P2 "not started, on Carl's go" but commit `797855ff feat(monthly-checkin): Phase 2` landed 07-13 23:41 | docs/plans/doing/monthly-one-on-one/plan.md L82 | Plan lags code (built-not-walked, most likely) | **NOT NOW** — live session's track; mention to owner | 🔴 foreign |
| P | planner-grounding Scope A superseded by thread-follow (closed 07-11); shared-shell-layer self-declares it may duplicate the shared-folder-split track | docs/plans/future/planner-grounding/, shared-shell-layer/ | Both self-flagged, awaiting reconcile | Decision: close/re-scope A; fold shared-shell-layer into the code-health track | 🟡 decision |
| — | ux-audit-fixes is a live in-flight track but its folder is untracked | docs/plans/doing/ux-audit-fixes/ (`??`) | Track not in git | **NOT NOW** — created today by a live session; it should commit | 🔴 foreign |

Live tracks check: monthly-one-on-one **active**, admin-live-deploy **active but merge-blocked**,
promises-loop cooling (P2/P3 open — **not safe to archive yet**), ux-audit-fixes just created.
Future/: design-stage-native still **not archive-safe** (P1 built, never walked); run-qa-fixes,
questions-outcome-moat, design-cleanups = legit parks; briefing-readability-p0 cold ~18 days (not superseded).

## Lens C — Git mess

| # | What | Where | Why stale | Suggested move | Risk |
|---|------|-------|-----------|----------------|------|
| A | ~90 untracked `content/questions/_runtime/*.yaml` drown every `git status`; the fix (a `_runtime/**` gitignore rule) is already named as parked in both trackers | .gitignore | Rule agreed, never landed | Land the one-line gitignore rule (does NOT delete any file) | 🟢 |
| B | `.claude/skills/clean-up/` + `.claude/skills/dependency-check/` still untracked since 07-12 — real repo tooling (this very sweep runs on one of them) | .claude/skills/ | Tooling never committed | Commit just these two paths | 🟢 |
| — | `main` and `work/monthly-checkin` have DIVERGED with duplicate commits (Team P1/P2, landing fix, focus-trap fix exist on BOTH with different hashes); monthly-checkin feature commits are NOT on main; local main behind origin/main by 1 (Pulse dashboard) | branches | Cherry-pick/rebase churn between sessions | **NOT NOW** — untangle only when the owning sessions settle; then merge/fold work/monthly-checkin properly | 🔴 foreign |
| — | ~35 modified/staged files = live sessions' in-flight work (monthly-checkin refactor, tracker refresh, prototype close, Start Sero.bat, tasks.js…) | working tree | Not stale — in-flight | Untouched; their sessions commit | 🔴 foreign |

No stashes. No orphan worktrees. No merged-stale branches.

## Lens D — Old & orphaned files + repo layout (Carl's core ask)

| # | What | Where | Why untidy | Suggested move | Risk |
|---|------|-------|-----------|----------------|------|
| C | Tombstone stub: "The board — moved… retired" (779-byte redirect) | docs/board.md (tracked) | Dead pointer; STATUS/SERO_BOARD are canonical | Delete (git keeps history) | 🟢 |
| D | Loose design-concept pair at docs/ top level; the PNG is the repo's largest tracked binary (1.3 MB), referenced by nothing | docs/ONE-ON-ONE-RUNNER-CONCEPT.md + docs/newrunnerdesign.png | One-off snapshot from 07-10, superseded by the built runner | Move both to docs/archive/ (keeps them, clears docs/ root) | 🟢 |
| E | Deploy how-to sitting at repo root | RENDER_SETUP.md | Root clutter; belongs with reference docs | Move to docs/reference/ | 🟢 |
| F | 244 MB untracked local run logs: logs/june/ 138 MB + logs/july/ 106 MB (+ loose generated files at logs/ root) | logs/ | Disk bloat + status noise | Zip logs/june/ into docs/archive/logs/ per the existing pattern, delete the folder; keep july (current month) | 🟡 zip first, then delete |
| G | Untracked `testing/` dir at root (test-plan.md, tester-pack.md, results/) — in limbo since 07-13, shows in every git status | testing/ | Neither committed nor ignored | Move under docs/testing/ and commit (or gitignore if scratch) | 🟡 confirm no session owns it |
| O | Rename darren→serolocal was scheduled "tonight 2026-07-08" — 7 days later repo + darren.code-workspace still old name; handover doc now misleading | docs/rename-serolocal-handover.md, darren.code-workspace | Rename stalled | Decision: re-schedule the rename (script is ready) or archive the doc for now | 🟡 decision |
| — | images/ at root: 5 Pexels stock photos + logo (2 MB), not obviously used by app code | images/ (tracked) | Root clutter | Verify usage → move under docs/assets/ or delete unused | 🟡 check refs first |
| — | docs/reports/ is 11 files flat: living dashboards (sero-*.html) mixed with dated one-offs | docs/reports/ | No sub-structure | Proposal: dated one-offs → docs/reports/archive/ or subfolders | 🟡 S effort |

No genuine junk files anywhere (zero .tmp/.bak/.orig/copies — 3rd sweep running).

## Lens E — Stale systems & dead code

| # | What | Where | Evidence | Suggested move | Risk |
|---|------|-------|----------|----------------|------|
| H | Dead stage screen (242 lines): PREPARATION routes to frontend .ts (main.js:46 comment says "old screen kept"); zero importers; 3 string refs now LIE (dev-badge tells devs the wrong file) | admin/src/stages/preparation.js + dev-badge.js:10, review-serialize.js:24,26, guide.js:91 | Grep both apps: no imports | Delete + correct the 3 label refs | 🟡 |
| I | Phase-006 scaffold repo whose only importer is its own test; the org-fence now lives in runs.service | backend/api/services/auth/org-data.repo.ts (+ org-data.test.ts) | git grep: zero production refs | Delete both (verify no planned reuse) | 🟡 |
| J | Likely-dead scripts: build-reviews.js (no refs), seed-manager.ts (no refs, unlike its siblings), benchmark.js cluster (4 files, manual paid tool, unwired) | scripts/ | Referenced by nothing live | Delete build-reviews + seed-manager; Carl calls benchmark cluster | 🟡 |
| K | member-runs.css exists but is NOT imported in the design.css barrel — orphaned or silently unloaded | admin/src/styles/design/member-runs.css | Barrel imports end at run-detail.css + mobile.css | Wire it up or delete it | 🟡 verify |

npm scripts: all resolve ✅. Env vars: none dead ✅ (LIVE_DATABASE_URL is deploy-time by design).
Cleared false traps: start-core.js, intake-firstrun.ts, run-debrief.d.mts, shared/api.js+sse.js — all live in at least one app.

## Lens F — Refactor opportunities (proposals only, never done inside a clean-up)

| # | What | Where | Effort | Why it matters |
|---|------|-------|--------|----------------|
| N1 | App-shell fork drift: main.js / router.js / app-nav.js each copied admin→frontend and diverging (208/84/116 identical lines); frontend also reaches into admin/src internals from 7+ stage files | admin/src + frontend/src | L | Fixes to one shell silently miss the other; shared UI belongs in shared/ |
| N2 | Four files > 800 lines: reviewer.ts (916), universe.ts (824), runs-store.ts (821), run-history.ts (801) | backend + admin | L | Hard to navigate/test; natural split points exist |

Debug logging: **clean ✅** (zero console.log in browser source). Commented-out code: **clean ✅**.

## Lens G — Styling drift (vs DESIGN.md)

| # | What | Where | Why drift | Suggested move | Risk |
|---|------|-------|-----------|----------------|------|
| L | The monthly-checkin runner exists TWICE and has drifted: shipped frontend/src/stages/guided/ (80 hexes, wrong slate fallbacks) vs old inline prototype admin/src/stages/tests/monthly-checkin.js (76 hexes + sub-14px text at 10 lines: 11/12.5/13/13.5px pills & labels) | guided.css + monthly-checkin.js | Same component, two homes; the prototype never got the 14px fixes | Retire the old prototype (or point it at guided.css) — clears the worst of hex + font-floor debt in one move | 🟡 |
| M | Hex-fallback drift epidemic: `var(--token, #wrongHex)` fallbacks are Tailwind-slate, not Sero (guided.css, error-log.css, feedback-inbox.css); tasks.js hardcodes 8 lane hexes that ARE token values; raw `#fff`/`#5aa9e6`/`#eef1f6` in buttons-inputs.css + app-nav.css | admin/src/styles + frontend guided.css | Tokens-only rule (DESIGN.md §2) | One styling-debt pass: strip fallbacks, point lanes at tokens | 🟡 M effort (tasks.js part = 🔴 file is live-modified) |

Skeletons: **clean ✅** — all four loaders use createSkeleton(). universe.ts/design.js/dev-badge: exempt per DESIGN.md §6.

## 😤 Looks bad but is actually fine
- ~90 untracked `content/questions/_runtime/*.yaml` — runtime question-bank artifacts, NEVER junk (standing rule). Pick A hides them from git status without touching them.
- The ~35-file dirty working tree — live parallel sessions mid-work (monthly-checkin, tracker refresh, prototype close), not mess.
- Deleted `docs/plans/future/monthly-one-on-one/*` + onboarding files — a live session correctly removing the superseded future/ copy.
- `docs/chat-history/` + `scripts/chat-log.py` — gitignored local tooling, deliberate.
- ~50 `scripts/test-*.js` — real test suites auto-discovered by run-tests.js, not scratch.
- `logs/may/` pinned run folders + the two archive .zips — deliberate keep-set.
- `LIVE_DATABASE_URL` read by no code — by design (becomes DATABASE_URL at deploy).
- admin/frontend cross-imports (~30) — the deliberate split pattern, not copy-paste.

## Pick-list

**Quick tidies 🟢 (safe, reversible, small):**
- **A** — land the `_runtime/**` gitignore rule (kills ~90 git-status noise lines, deletes nothing)
- **B** — commit the two skill folders (clean-up + dependency-check)
- **C** — delete the docs/board.md tombstone stub
- **D** — move the runner-concept pair (concept .md + 1.3 MB png) to docs/archive/
- **E** — move RENDER_SETUP.md to docs/reference/

**Bigger tidies 🟡 (safe with one check each):**
- **F** — zip logs/june/ (138 MB) into docs/archive/logs/, then delete the folder
- **G** — adopt the limbo testing/ dir → docs/testing/ + commit
- **H** — delete dead preparation.js + fix its 3 stale label refs
- **I** — delete org-data.repo.ts + its test
- **J** — delete build-reviews.js + seed-manager.ts (benchmark cluster = your call)
- **K** — wire up or delete the orphaned member-runs.css
- **L** — retire the old inline monthly-checkin prototype (guided.css is the live one)
- **M** — styling-debt pass: strip wrong hex fallbacks (skipping live-modified tasks.js)

**Decisions 🟡 (nothing moves until you call it):**
- **O** — rename darren→serolocal: re-schedule or shelve the handover doc
- **P** — reconcile planner-grounding (Scope A shipped) + fold shared-shell-layer duplicate
- **N** — park the two big refactor proposals (shell-fork drift, 800-line files) as a future plan

**Standing options:** **ALL 🟢** (= A+B+C+D+E) · **NOTHING** (sweep only)

**🔴 Not now (foreign — live sessions own these):** tracker fixes (SERO_BOARD 5→4 etc.), the
manager-workspace close commit, the main↔work/monthly-checkin branch dedupe, Start Sero.bat move,
tasks.js styling. Re-check next sweep.
