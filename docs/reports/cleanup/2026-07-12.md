# Clean-up sweep — 2026-07-12

First run of the clean-up skill — everything below is **NEW** (no previous report to diff).
Read-only sweep; nothing was changed. HEAD at sweep time: `0f53523c` (moved during the sweep —
live sessions are committing; re-verify before any Pass-2 action).

## Lens A — TODOs & promises in code
**Clean ✅** — zero TODO / FIXME / HACK / XXX markers across admin/src (127 files),
frontend/src (16), backend (300), shared (3), scripts (80). Cross-checked two grep methods.

## Lens B — Trackers & unfinished work

| # | What | Where | Why stale | Suggested move | Risk |
|---|------|-------|-----------|----------------|------|
| A1 | STATUS.md missing 3 of 4 live tracks (promises-loop, admin-live-deploy, design-stage-native); ~18 ✅-closed tracks still listed despite its own "only live work" rule | STATUS.md | Trackers lag a day behind actual in-flight work | Refresh: add live tracks, move closed ones out | 🟢 |
| A2 | SERO_BOARD.md "Now — open work" contains only closed items; none of the 4 active tracks; test counts say 57/57 (actual ~124–126) | SERO_BOARD.md §1, lines 199/261 | Same lag | Refresh rows + counts | 🟢 |
| B | design-stage-native track quietly dead since 2026-07-10: P1 built, **awaiting Carl's QA walk**; P5 blocked on parity sign-off | docs/plans/doing/design-stage-native/ | No activity while 3 newer tracks took over | **NOT safe to archive** — walk it or consciously park to future/ | 🟡 decision |
| C | planner-grounding future plan's fix A ("drill, don't switch") appears already shipped by the thread-follow track (closed 2026-07-11) | docs/plans/future/planner-grounding/plan.md | Partially superseded | Reconcile: close or re-scope to still-open items (B: grounded bank, C: low-confidence padding) | 🟡 |
| — | Overlap notes (informational): promises-loop now owns the `outcomeCheck` consumer that questions-outcome-moat listed as a seed; monthly-one-on-one's promise-persistence scope partially carved out by promises-loop | future/ plans | — | Note in plans so nothing double-builds | ℹ️ |

Untracked plan folders `docs/plans/doing/promises-loop/` and `admin-live-deploy/` belong to live
sessions (created today) — their sessions should commit them; reported, not touched.

## Lens C — Git mess

| # | What | Where | Why stale | Suggested move | Risk |
|---|------|-------|-----------|----------------|------|
| D | Branch fully merged into main | `claude/eloquent-dijkstra-c863be` | Merged, serves no purpose | Delete branch | 🟢 |
| E | Two leftover agent worktrees, both CLEAN (no uncommitted work), detached HEAD | `.claude/worktrees/unruffled-gauss-88b854`, `zealous-mclaren-70cce7` | Parallel-session leftovers; also slow down full-repo greps | `git worktree remove` both | 🟢 |
| F | Unmerged branch with ONE commit: `fix(member-runs): type getRunsAboutMe result so admin typecheck passes` (271143fc) | `claude/laughing-williams-586fa7` | Orphaned fix — may already be superseded on main | Verify still needed → merge or drop | 🟡 decision |
| — | ~35 modified + ~15 untracked working-tree files owned by LIVE sessions (past-1on1-view P2, promises/promise-confirm, internal-tool-guard, run-detail, scenario-batch deletes, test-gallery stage) | working tree | Not stale — in-flight | Untouched; their sessions commit them | 🔴 foreign |
| — | `work/monthly-one-on-one` worktree at serolocal-monthly-one-on-one — clean, active | worktree | Not stale | Leave | ℹ️ |

No stashes.

## Lens D — Old & orphaned files

| # | What | Where | Why stale | Suggested move | Risk |
|---|------|-------|-----------|----------------|------|
| G | 1.3MB PNG at docs/ root, referenced by nothing | docs/newrunnerdesign.png (tracked) | One-off design snapshot | Delete (git keeps history) or move to docs/archive/ | 🟡 check it's not a live design ref |
| H | Local gitignored piles: `.playwright-mcp/` (~3.5MB tool output), 4 root screenshots, old `logs/june|july|gate|benchmark` | local only | Tool/runtime output | Optional local clear — never touches git | 🟢 local |

Otherwise clean: no *.tmp/*.bak/*.orig/copies anywhere; all 45 `scripts/test-*.js` are a real
auto-discovered suite; docs/archive/ is deliberate.

## Lens E — Stale systems & dead code

| # | What | Where | Why stale | Suggested move | Risk |
|---|------|-------|-----------|----------------|------|
| I | `admin/src/stages/preparation.js` (242 lines) has ZERO importers — both apps route PREPARATION → frontend/src/stages/preparation.ts; only string-label refs remain (dev-badge.js:10, review-serialize.js:24, guide.js:91) | admin/src/stages/preparation.js | Superseded; comment says "old screen kept" (intentional-pending) | Delete + update the 3 label refs | 🟡 Carl confirms |
| J | 6 env vars read but documented nowhere: SERO_ENV (legacy alias of APP_ENV), DEV_AUTOLOGIN, AI_MAX_CONCURRENCY, GUEST_RUNS_PER_DAY, OPENAI_MODEL, RUN_FILE_ECHO | backend (various, see agent evidence) | Undocumented knobs | Document in .env.example; consider dropping SERO_ENV after confirming no deploy sets it | 🟢 document |

All npm scripts resolve; every other suspected-dead module has live importers (checked both apps).

## Lens F — Refactor opportunities (proposals only)

| # | What | Where | Effort | Suggested move | Risk |
|---|------|-------|--------|----------------|------|
| K | admin↔frontend shell copies hand-synced and already drifted: main.js (411 vs 352), router.js (164 vs 122), app-nav.js (333 vs 203) — every new stage/nav change must land twice | both apps' main.js/router.js/ui/app-nav.js | M–L | Park as a future plan (shared shell layer) | 🟡 park |
| L | Two full prep-brief implementations over the same SSE stream (frontend .ts rewrite vs admin .js kept) — merges with finding I | preparation.js/.ts | L | Resolved if I is done | 🟡 |
| — | Stray console.logs worth a glance: server.ts (6), api/sessions.ts (1), session-persistence.ts (1), run-debrief.mjs (23) — everything else is intentional CLI output | backend | S | Optional tidy | 🟢 |
| — | Oversized files (informational): reviewer.ts 916, universe.ts 824, runs-store.ts 821, run-history.ts 801, role-profile.ts 748, sessions.service.ts 743 | backend + admin | — | No action; context for future splits | ℹ️ |

## Lens G — Styling drift (vs DESIGN.md)

| # | What | Where | Why drift | Suggested move | Risk |
|---|------|-------|-----------|----------------|------|
| M | Worst offender on every axis: hardcoded non-Sero Tailwind palette (KB_LANE_COLORS line 35, --kb-lane hexes 547-8), off-palette slate token fallbacks, 11px sync-dot (line 567, below 14px floor), ~31 inline CSS selectors despite tasks-board.css existing | admin/src/stages/tasks.js | Breaks tokens-only rule + 14px floor | Fix — **but file is live-session-modified right now** | 🔴 not now → park |
| N | ~73 inline CSS selectors + ~18 literal hexes (dark-space viz), no universe.css; meeting-arcs.js similar (~54 inline selectors) | admin/src/stages/universe.ts:38-108, meeting-arcs.js | Feature CSS not in own file; undeclared token exemption | Extract to feature CSS or declare exemption in DESIGN.md | 🟡 |
| — | Sub-14px in test chrome: monthly-checkin.js (11px + 13px ×6), test-engine.css:10 (12px) — files owned by live session (untracked tests/) | admin/src/stages/tests/ | Below floor, but internal test chrome | Note for the owning session | 🔴 foreign |

Skeleton loaders: clean ✅ (everything uses createSkeleton()). design.css: clean ✅ (proper 27-file @import manifest).

## 😤 Looks bad but is actually fine
- `content/questions/_runtime/*.yaml` (55 untracked) — runtime question-bank artifacts, committed by convention, NEVER junk.
- `docs/chat-history/` + `scripts/chat-log.py` — gitignored local tooling, deliberate.
- `admin/src/stages/test.js` + `stages/tests/` — the intentional /test prototype gallery, live feature work.
- `logs/may/` — whitelisted regression baseline in .gitignore.
- `docs/rename-serolocal-handover.md` — not stale: the rename is genuinely still pending (folder is still "darren").
- backend CLI console.logs (cli.ts 44, briefing.ts 26 etc.) — intentional pretty-print output.
- `LIVE_DATABASE_URL` in .env.example — deliberate "parking spot", never read by design.
- ~50 working-tree changes — live parallel sessions, not mess.
